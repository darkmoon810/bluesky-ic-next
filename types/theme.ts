export interface ThemeSettings {
  colors: {
    primary: string
    secondary: string
    text: string
    background: string
    backgroundAccent: string
    hover: string
  }
  fonts: {
    serif: string
    sans: string
  }
}